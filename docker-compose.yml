version: "3.7"
services: 
  builder:
     context: .
     build: ./images/builder
     working_dir: /var/app
     volumes: 
       - ./app/:var/app
     links:
       - prodworker
  
  prodworker:
    image: alpine:latest 
    volumes:
      - ./app/:var/app